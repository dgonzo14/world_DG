import type { Home } from '../geo/atlas'

/** Where the route starts and ends, and what distances are measured from. */
export const HOME: Home = {
  code: 'STL',
  name: 'St. Louis',
  lat: 38.627,
  lng: -90.199,
}
